[project]
name = "cloud-101"
version = "0.1.0"
description = "Cloud 101 Workbench — a foundations course for Google Cloud"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
